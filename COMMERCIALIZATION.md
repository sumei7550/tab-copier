# Tab Copier 商业化功能说明（第四阶段）

## 产品边界

Tab Copier 的免费版应该让用户在第一次安装后立刻完成一次高频任务：选择当前窗口中的标签页，并复制为 Markdown、纯文本或 URL。这个闭环必须保持无账号、无云端、无支付、无远程服务器，并且所有处理都在浏览器本地完成。

| 模块 | 免费版 | Pro 预留 |
| --- | --- | --- |
| 标签页范围 | 当前窗口 | 全部窗口 |
| 选择与预览 | 基础选择、全选、筛选、预览 | 在现有基础上扩展批量工作流 |
| 输出 | Markdown、纯文本、URL，复制到剪贴板 | 文件导出 |
| 整理 | 保持浏览器标签页顺序 | 去重、按域名分组 |
| 模板 | 内置固定格式 | 自定义模板 |
| 工作集 | 不保存 | 标签页集合保存与恢复 |
| 浏览器结构 | 不承诺保留分组 | 保留 Chrome 标签页分组 |
| 数据与账户 | 本地处理、无账号、无同步 | 仍应优先保持本地；不因 Pro 引入云端依赖 |

## Feature flag 约定

`feature-flags.js` 是唯一的能力边界入口。当前 `chrome.storage.local` 只读取 `plan`，默认值为 `free`；没有支付、远程校验或账号状态。复制流程只依赖 capability，不依赖未来的授权实现。

当前预留 capability：`allWindows`、`deduplicate`、`domainGrouping`、`fileExport`、`customTemplates`、`tabCollections`、`chromeTabGroups`。以后接入授权时，只替换 plan 的来源，并保留默认关闭与降级到免费版的行为。

## 哪些功能值得付费

最适合付费的是能明显节省整理时间、且不影响免费用户第一次成功体验的能力：全部窗口复制、去重、按域名分组、文件导出、自定义模板，以及标签页集合的保存/恢复。Chrome 标签页分组保留也适合放在 Pro，因为它服务于重度研究和项目管理场景。

不建议把复制按钮、三种基础格式、当前窗口选择、筛选、预览或本地处理设为付费墙。这些是安装转化和口碑传播的“第一次价值”，应让用户在无需账号和无需配置的情况下立即体验。

## 建议价格区间

建议先用一次性买断作为第一版商业模型，减少隐私产品对订阅和服务器的解释成本：

- 个人版：一次性 9.99–19.99 美元，或人民币 68–128 元。
- 重度用户/专业版：一次性 24.99–39.99 美元，或人民币 168–268 元；仅在模板、集合、分组保留等能力成熟后考虑。
- 如果后续确实需要跨设备同步，再单独评估低价订阅；当前阶段不要为同步提前引入账号或服务器。

价格应在真实使用数据和用户访谈后验证，不应在未接入授权前把本地 `plan` 开关当作销售机制。

## 建议推进顺序

1. 先完成本地能力模型和 UI 状态（当前阶段）。
2. 优先实现文件导出、去重、按域名分组三个最容易感知价值的 Pro 能力。
3. 再实现模板和集合保存/恢复，并设计导入导出格式，避免数据被锁在扩展内部。
4. 最后实现 Chrome 标签页分组保留；确认浏览器 API 行为后再决定是否纳入首发 Pro。

